---
import { getCollection } from 'astro:content'
import BaseLayout from '../../layouts/BaseLayout.astro'
import EventCard from '../../components/EventCard.astro';


const events = (await getCollection('termine'))
                .filter(event => new Date() <= new Date(event.data.date))
---

<BaseLayout title='Termine'>
  <h1 class="my-5">Kommende Termine</h1>
  
  {events.length > 0 ? (
    <ol class="row p-0">
      {events.map(event => (
        <li class="my-2" style={{'listStyleType': 'none'}}>
          <EventCard event={event} />
        </li>
      ))}
    </ol>
  ) : (
    <p>nix los in Grönau 🫥</p>
  )}
</BaseLayout>
